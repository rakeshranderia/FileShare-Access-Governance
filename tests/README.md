# Tests

`AccessGovernance.Tests.ps1` provides a starter Pester suite for pure/static behaviours and documents the AD/ACL cases that should be mocked in a Windows CI or lab environment. Production acceptance still requires a representative Windows/AD test environment.
