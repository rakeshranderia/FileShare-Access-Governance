# Diagrams
The publication uses Mermaid diagrams embedded in the Markdown documentation so they render natively on GitHub and remain version-controlled as text. Source diagrams are in the corresponding documents under `/docs`.
